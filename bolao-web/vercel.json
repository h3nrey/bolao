{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "${BACKEND_URL}/$1",
      "env": ["BACKEND_URL"]
    },
    {
      "src": "/((?!.*\\.).*)",
      "dest": "/index.html"
    }
  ]
}
